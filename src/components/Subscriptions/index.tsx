import { Card } from "rbx";
import React from "react";
import "./styles.scss";


interface Props extends React.ComponentPropsWithoutRef<"div"> {
}

const Subscription: React.FC<Props> = (p) => {
    return(
        <div>
            Under construction
        </div>
    )
}

export default Subscription;
